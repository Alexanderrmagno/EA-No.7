import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Evidencia de Aprendizaje No.7',
  webDir: 'www'
};

export default config;
